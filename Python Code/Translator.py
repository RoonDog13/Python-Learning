from string import whitespace, punctuation
user_input = input("What do you want to be translated? ")
input_line = user_input.lower().split()
dictionary_key = {"want": , "translated:

for element in input_line:
    if element == "want":
        #remove want and insert something else
    if element == "translated":
        #remove translated and insert something else

